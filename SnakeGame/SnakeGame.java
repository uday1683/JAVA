package SnakeGame;

public class SnakeGame {
    public static void main(String[] args) { // This class will contain the main method [2]
        // Create an instance of GameFrame.
        // Using a shortcut as the named instance 'frame' is not used elsewhere [4].
        new GameFrame(); // Creates and displays the game window [4]
    }
}